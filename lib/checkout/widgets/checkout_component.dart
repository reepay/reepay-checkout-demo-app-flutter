import 'package:flutter/material.dart';

class CheckoutComponent extends StatelessWidget {
  const CheckoutComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}