import 'package:flutter/material.dart';

class AuthComponent extends StatelessWidget {
  const AuthComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}